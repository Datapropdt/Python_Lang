# if condition : the condition must be terminated with : and else must be
# teminated with : ,true false statements must be given one tab space
# input() method is used to get the input from keyboard at runtime
# staced if : if an if condition contains only true part then it is stacked if
#Syntax for if: if (<condition>):
#                       <true stmt>


age =int(input("enter your age "))
if ((age)>18):
 print("you can vote")  